{
  "identifier": "default",
  "description": "Default capabilities for the BMS Monitor app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
